class Session{
    constructor(session, gameCanvas, dropObject){
        this.session = session;
        this.start(this.session)
    }

    start(sessionID){
        switch(sessionID){
            case 1:
                break;
            case 2:
                break;
        }
    }
}